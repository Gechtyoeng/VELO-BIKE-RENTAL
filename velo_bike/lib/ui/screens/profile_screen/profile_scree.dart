import 'package:flutter/material.dart';

class ProfileScree extends StatelessWidget {
  const ProfileScree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('this is profile screen');
  }
}